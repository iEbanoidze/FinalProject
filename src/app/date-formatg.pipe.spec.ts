import { DateFormatgPipe } from './date-formatg.pipe';

describe('DateFormatgPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatgPipe();
    expect(pipe).toBeTruthy();
  });
});
